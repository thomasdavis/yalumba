{
  "name": "@yalumba/fastq",
  "version": "0.0.1",
  "description": "Streaming FASTQ parser with zero-copy slicing and packed encoding",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@yalumba/io": "workspace:*",
    "@yalumba/genome": "workspace:*"
  }
}
