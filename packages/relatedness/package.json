{
  "name": "@yalumba/relatedness",
  "version": "0.0.1",
  "description": "Genome relatedness metrics — shared segments, IBD estimation, centimorgan approximation",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@yalumba/kmer": "workspace:*",
    "@yalumba/alignment": "workspace:*"
  }
}
